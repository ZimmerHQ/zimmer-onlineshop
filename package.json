{
  "name": "insta-backend-unified",
  "version": "1.0.0",
  "description": "Unified backend and frontend for Instagram automation",
  "main": "start_server.py",
  "scripts": {
    "start": "python start_server.py",
    "dev": "python start_server.py",
    "build": "cd frontend && npm install && npm run build",
    "install-deps": "pip install -r requirements.txt && cd frontend && npm install"
  },
  "keywords": ["fastapi", "nextjs", "instagram", "automation"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
} 